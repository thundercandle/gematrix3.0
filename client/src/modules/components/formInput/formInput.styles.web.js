// Styling for web

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  inputField: {
    height: 40,
    borderColor: 'none',
    borderBottomWidth: 1,
    borderBottomColor: '#fff',
    color: '#fff',
    padding: 5,
    fontSize: 18
  }
})
