type Word {
    entry: String!
    # A word will always value out to a number
    numeral: Numeral! @belongsTo
    # A word is composed of particular letters that can be accessed
    letters: [Letter] @hasMany
    language: String
    pronunciation: String!
    definition: String!
    notes: [Note] @hasMany
    see: [String]
}