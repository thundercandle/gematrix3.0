# A correpondence is a certian symbolic link, part of a set, and is associated with a letter, a number, or both
type Correspondence {
    # A correspondence can be added to many sets
    set: [Set] @hasMany
    content: String!
    numeral: Numeral @hasOne
    letter: Letter @hasOne
    notes: [Note] @hasMany
}